import ShopModernMain from '@/pages/homes/shop-modern/ShopModernMain';
import { Metadata } from 'next';

export const metadata: Metadata = {
    title: "SAB Creative - Shop Modern Light",
};

const page = () => {
    return (
        <ShopModernMain />
    );
};

export default page;